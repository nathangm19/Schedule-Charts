Run the attached Python script EXACTLY as written. Do not modify it and
do not design your own charts - the four template files ARE the approved
dashboard design; the script only refreshes the data inside them.

Files attached with this prompt:
  1. build_dashboards.txt     (the script)
  2. Planner_Extract.xlsx     (step 1's FULL extract - not the week file)
  3. crew_index.html          (template)
  4. crew_app.js              (template)
  5. gantt_head.html          (template)
  6. gantt_app.js             (template)

Do this:
  1. Save build_dashboards.txt as build_dashboards.py and the four
     templates under exactly those names.
  2. Run the script with Python in that folder.
  3. Show me its printed report (workdays, bars, window, clipped count).
  4. Give me both outputs to download:
       SNT-Crew-Loading.html   (crews per day, stacked by trade)
       SNT-Gantt.html          (waterfall Gantt with floating histogram)

If the script errors, show me the complete error text and stop.
Do not attempt a workaround.
